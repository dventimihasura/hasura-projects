table:
  name: account
  schema: public
array_relationships:
  - name: orders
    using:
      manual_configuration:
        column_mapping:
          id: account_id
        insertion_order: null
        remote_table:
          name: order
          schema: public
