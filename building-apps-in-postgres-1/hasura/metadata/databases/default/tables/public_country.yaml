table:
  name: country
  schema: public
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: country_id
        table:
          name: account
          schema: public
