table:
  name: frequent_flyer
  schema: postgres_air
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: frequent_flyer_id
        table:
          name: account
          schema: postgres_air
