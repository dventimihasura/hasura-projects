table:
  name: department
  schema: fmd
array_relationships:
  - name: user_departments
    using:
      foreign_key_constraint_on:
        column: department_id
        table:
          name: user_department
          schema: user
