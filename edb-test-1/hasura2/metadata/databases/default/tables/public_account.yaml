table:
  schema: public
  name: account
array_relationships:
  - name: miscs
    using:
      foreign_key_constraint_on:
        column: account_id
        table:
          schema: public
          name: misc
  - name: orders
    using:
      foreign_key_constraint_on:
        column: account_id
        table:
          schema: public
          name: order
