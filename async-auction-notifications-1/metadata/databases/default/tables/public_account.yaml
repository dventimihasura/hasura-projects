table:
  name: account
  schema: public
array_relationships:
  - name: routes
    using:
      foreign_key_constraint_on:
        column: account_id
        table:
          name: route
          schema: public
