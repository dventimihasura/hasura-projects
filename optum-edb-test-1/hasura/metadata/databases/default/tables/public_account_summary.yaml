table:
  schema: public
  name: account_summary
object_relationships:
  - name: account
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: account
        insertion_order: null
        column_mapping:
          id: id
